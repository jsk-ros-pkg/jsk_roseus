#!/bin/bash -x

TARGET="\"$1\"";
shift
# argc
DEPENDS=""
while [ $# -ne 0 ]; do
    DEPENDS=$DEPENDS"\"$1\" ";
    shift;
done
if [ -e `rospack find euslisp`/eus-installed ] ; then
    rosrun euslisp eus2 "(progn (load \"`rospack find roseus`/scripts/genmanifest.l\")(genmanifest $TARGET $DEPENDS)(exit))"
else
    echo ";; rosmake eusilsp !!"
fi





